<% places.forEach((place)=>{%>


<%- include('../single/mini-card', { key : place._id, 
                                          title : place.name, 
                                          img : place.name.image('places'), 
                                          subTitle: place.staminaCost.format().concat(word.STAMINA), 
                                          badgeText: place.successChance.percent()
                                        }); %>

<%- include('../placeholders/mini-card'); %>

<% }); %>