<div class="card-flat">
  <div id="escape-holder" class="row">
    <div class="col-4">
      <h6 class="card-title"><%=text.PRISON.TRY_ESCAPE%></h6>
      <p class="card-text"><%=text.PRISON.SOME_GUY_HELP%></p>
      <img src=" /img/prisoner.png" class="card-img-top medium-img" />
    </div>

    <div class="col-8">

      <%- include('../single/label-item', { id: 'escape-coins', text: word.COINS_COST,  icon: 'coin', showOnlyIf : escape.update.coins.abs().positive(), value: escape.update.coins.format()}); %>

      <%- include('../single/label-item', { id: 'escape-respect', text: word.RESPECT.concat(word.INCREASE),  icon: 'respect', showOnlyIf : escape.update.respect.abs().positive(), value: escape.update.respect.sign()}); %>

      <%- include('../single/label-item', { id: 'escape-chance', text: word.ESCAPE_CHANCE,  icon: 'lucky', value: escape.escapeChance.percent()}); %>

      <%- include('../single/label-item', { id: 'escape-stamina', text: word.STAMINA_COST,  icon: 'stamina', showOnlyIf : escape.update.stamina.abs().positive(), value: escape.update.stamina.abs().percent()}); %>


      <%- include('../single/buttons/button-group', { id: 'escape', 
                                        text: word.ESCAPE,  
                                        quoteText: text.PRISON.ADDING_DAYS_JAIL.format(escape?.daysIncOnFail, null), 
                                        quoteDefault : text.PRISON.ADDING_DAYS_JAIL,
                                        loadingButton : true, 
                                        insufficientCoins: escape.update.coins.abs().greater(player.coins), 
                                        noStamina: escape.update.stamina.abs().greater(player.stamina), 
                                        lifeImprisonment: player.isLifeImprisoned() 
                                     }); %>



    </div>

  </div>
</div>