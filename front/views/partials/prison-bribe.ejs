<div class="card-flat">
  <div id="bribe-holder" class="row">
    <div class="col-5">
      <h6 class="card-title"><%=text.PRISON.LOOKGIN_FOR%></h6>
      <p class="card-text"><%=text.PRISON.CAN_TAKE_IT_EASY%></p>
      <img src=" /img/police.png" class="card-img-top medium-img" />
    </div>
    <div class="col">

      <%- include('../single/label-item', { id: 'bribe-coins', text: word.COINS_COST,  icon: 'coin', showOnlyIf : bribe.data.coins.abs().positive(), value: bribe.data.coins.format()}); %>

      <%- include('../single/label-item', { id: 'bribe-respect', text: word.RESPECT_REWARD,  icon: 'respect-star', showOnlyIf : bribe.data.respect.positive(), value: bribe.data.respect.sign()}); %>

      <%- include('../single/label-item', { id: 'bribe-stamina', text: word.STAMINA_COST,  icon: 'stamina', showOnlyIf : bribe.data.stamina.abs().positive(), value: bribe.data.stamina.abs().percent()}); %>

      <%- include('../single/button', { id: 'bribe', 
                                        text: word.BRIBE,  
                                        quoteText: text.PRISON.GET_OUT_CHANCE.format(bribe.escapeChance), 
                                        loadingButton: true, 
                                        insufficientCoins: bribe.data.coins.abs().greater(player.coins),
                                        noStamina: bribe.data.stamina.abs().greater(player.stamina),
                                        lifeImprisonment: player.lifeImprisonment 
                                     }); %>

    </div>
  </div>
</div>