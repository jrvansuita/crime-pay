<% hookers.forEach((hooker)=>{%>

<%- include('../single/mini-card', {      dataKey : hooker._id, 
                                          title : hooker.name, 
                                          img :  hooker.name.image('hookers'),  
                                          subTitle: hooker.stamina.sign().concat(word.STAMINA), 
                                          badgeText: hooker.coins.abs().format(word.FREE), 
                                          badgeImg:  hooker.coins ? "/img/coin.png" :null 
                                        }); %>

<%- include('../placeholders/mini-card'); %>

<% }); %>