<% weapons.forEach((weapon)=>{%>

<%- include('../single/big-card-item', { key : weapon._id, 
                                          name : weapon.name, 
                                          img :  weapon.name.image('merchandises','png'),  
                                          title: weapon.level.toString().concat(word.LEVEL), 
                                          badgeText: 'Equiped',
                                          imageClass: 'weapon-img',
                                          extraTitle : weapon.class,
                                          imgColor : weapon.color,
                                          sectionData: weapon.bundle,
                                          customSectionLayout: '../partials/weapon-card-section-lines'
                                        }); %>

<%- include('../placeholders/mini-card-item'); %>

<% }); %>