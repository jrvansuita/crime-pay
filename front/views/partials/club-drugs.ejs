<% drugs.forEach((drug)=>{%>

<%- include('../single/mini-card', {      dataKey : drug._id, 
                                          title : drug.name, 
                                          img :  drug.name.image('drugs'),  
                                          subTitle: drug.stamina.sign().concat(word.STAMINA), 
                                          badgeText: drug.coins.abs().format(word.FREE), 
                                          badgeImg:  drug.coins ? "/img/coin.png" :null 
                                        }); %>

<%- include('../placeholders/mini-card'); %>

<% }); %>